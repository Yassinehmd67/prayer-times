// components/method-picker.js — اختيار طريقة الحساب (مستقبلاً)

export function initMethodPicker(selectEl) {
  if (!selectEl) return;
  // في المستقبل يمكن توليد الخيارات ديناميكياً من METHOD_PRESETS
  console.log("method-picker stub attached");
}
